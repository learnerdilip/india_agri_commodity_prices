[tool.poetry]
name = "enam-core"
version = "0.1.0"
description = "ENAM Data Fetcher API"
authors = ["Your Name <diliptalks@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.109.0"
uvicorn = "^0.27.0"
requests = "^2.31.0"
pandas = "^2.2.0"
pymongo = { version = "^4.6", extras = ["srv"] }
pydantic-settings = "^2.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
